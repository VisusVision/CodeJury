# === Core Framework ===
fastapi[standard]
uvicorn[standard]
pydantic>=2.0
pydantic-settings
python-multipart

# === Database (PostgreSQL) ===
asyncpg

# === Sandbox (Docker Pool) ===
docker
requests

# === HTTP Client (Ollama) ===
httpx
